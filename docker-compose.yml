version: '3.7'

services:
  api:
    restart: always
    container_name: chat-ai
    build: .
    ports:
      - "3000:3000"
    networks: 
      - bridge
    volumes:
      - data:/var/www/chat         

networks:
  bridge:
    driver: bridge

volumes:
  data: