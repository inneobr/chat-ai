version: '3.7'

services:
  openai:
    restart: always
    container_name: openai
    build: .
    ports:
      - "3000:3000"
    networks: 
      - webservice
    volumes:
      - ./data:/data       

networks:
  webservice:
    driver: bridge